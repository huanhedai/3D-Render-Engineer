#ifndef CONFIG_H
#define CONFIG_H

// 资源目录路径（由CMake自动填充，无需手动修改）
#define RESOURCE_DIR "${PROJECT_SOURCE_DIR}/resource"
#define SHADER_DIR RESOURCE_DIR "/shaders"
#define TEXTURE_DIR RESOURCE_DIR "/textures"
#define MODEL_DIR RESOURCE_DIR "/models"

// 窗口默认配置
#define DEFAULT_WINDOW_WIDTH 800
#define DEFAULT_WINDOW_HEIGHT 600
#define DEFAULT_WINDOW_TITLE "PBR Renderer"

// 渲染相关默认值
#define DEFAULT_FOV 60.0f
#define DEFAULT_NEAR_PLANE 0.1f
#define DEFAULT_FAR_PLANE 1000.0f
#define MAX_POINT_LIGHTS 4

#endif // CONFIG_H